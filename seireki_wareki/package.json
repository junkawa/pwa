{
  "name": "wareki_seireki",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist && mkdir dist && cp -a src/* dist/ && workbox generateSW workbox-config.js",
    "deploy": "rm -rf ../docs/wareki_seireki && mkdir ../docs/wareki_seireki && cp -a dist/* ../docs/wareki_seireki/"
  },
  "author": "Jun Kawashima",
  "license": "MIT",
  "devDependencies": {}
}
